---
# 产品需求文档 (PRD)：个人基金收益监控 H5 应用
| 文档版本 | 修改日期 | 修改人 | 修改内容 | 备注 |
| :--- | :--- | :--- | :--- | :--- |
| V1.1 | 2026-09-16 | 产品经理 | 存储层升级为 IndexedDB；完善数据库设计；增加备份功能建议 | 优化数据结构与性能 |
---
## 1. 项目背景与目标
### 1.1 项目背景
用户持有的基金分散在不同平台（支付宝、天天基金、银行等），查看当日收益需要打开多个 APP，且各 APP 广告繁多、信息干扰大。目前缺乏一个轻量级、无广告、专注于“收益”和“趋势”的聚合监控工具。
### 1.2 项目目标
*   **核心价值**：提供一款极简的 H5 页面，让用户能在一分钟内看清当日所有持仓的盈亏情况。
*   **部署目标**：利用 GitHub Pages 进行零成本托管，生成一个 HTTPS 链接，确保在微信、浏览器中均可直接访问，无需下载安装。
*   **技术目标**：
    *   前端采用 SPA（单页应用）模式。
    *   **数据存储利用浏览器原生的 IndexedDB**，实现结构化数据存储，确保大数据量下的操作流畅性。
---
## 2. 用户角色
*   **个人投资者**：持有 1-10 只基金，主要关注“今天赚/亏了多少”，偶尔查看历史走势，对复杂的投资分析功能需求不强，追求“快”和“准”。
---
## 3. 功能需求
### 3.1 核心功能 MVP (Minimum Viable Product)
#### F1. 首次加载与引导
*   **需求描述**：用户首次打开页面时，若本地无基金数据，应显示“空状态”页面。
*   **功能点**：
    *   显示引导文案：“开始添加你的第一只基金”。
    *   提供“添加基金”的主按钮。
#### F2. 基金管理 (CRUD)
*   **需求描述**：用户可以自由添加、编辑和删除自己关注的基金。
*   **功能点**：
    *   **F2.1 添加基金**：
        *   输入框：基金代码（6位数字）。
        *   输入框：持有份额（数字，支持小数点）。
        *   交互：输入代码后，系统自动调取接口填充“基金名称”（若接口失败允许手动输入）。
        *   **存储逻辑**：点击保存时，将基金对象（Code, Amount, Name等）写入 IndexedDB 的 `funds` 对象仓库中。
    *   **F2.2 编辑基金**：允许修改持有份额（用于定投更新），修改份额后，更新 IndexedDB 中对应主键的数据。
    *   **F2.3 删除基金**：列表页提供删除入口，需二次确认（弹窗），确认后通过主键从 IndexedDB 中删除对应记录。
    *   **F2.4 数据读取**：应用启动时，异步从 IndexedDB 读取所有基金数据并渲染列表。由于是异步操作，需在数据加载完成前展示 Loading 骨架屏或加载动画。
#### F3. 首页监控看板
*   **需求描述**：这是最高频使用的页面，直观展示当日收益。
*   **功能点**：
    *   **F3.1 总盈亏展示**：
        *   顶部醒目位置显示“当日预估总收益”金额（例如：+125.60 元）。
        *   金额颜色逻辑：涨为红，跌为绿。
    *   **F3.2 基金卡片列表**：
        *   以卡片形式展示每只基金。
        *   内容包含：基金名称、基金代码、当前估算净值、当日涨跌幅（%）、当日盈亏金额（元）。
    *   **F3.3 数据刷新**：
        *   提供手动“刷新”按钮。
        *   每次打开页面自动刷新一次数据。
#### F4. 历史数据查看
*   **需求描述**：查看单只基金的历史净值走势。
*   **功能点**：
    *   **F4.1 走势图**：点击基金卡片，弹出详情页（或跳转新页面），展示净值走势折线图（使用 ECharts）。
    *   **F4.2 时间维度**：默认展示近 1 个月数据（MVP阶段），后续可扩展支持近3月、近1年切换。
    *   **F4.3 数据列表**：图表下方提供简单的历史净值日期列表（可选）。
---
## 4. 信息架构与用户流程
### 4.1 页面结构图
```
主页
├── 顶部导航：标题 + 刷新按钮
├── 总览卡片：当日总收益
├── 基金列表
│   ├── 基金卡片 A
│   ├── 基金卡片 B
│   └── "添加基金" 按钮
└── 底部版权/免责声明
添加/编辑基金页 (Modal/Page)
├── 输入：基金代码
├── 输入：持有份额
├── 显示：基金名称 (自动获取)
└── 按钮：保存 / 取消
基金详情页 (Detail View)
├── 顶部：基金名称 + 返回按钮
├── 图表区域：历史净值走势
└── 数据列表：近N日净值详情
```
### 4.2 核心用户流程
1.  **用户打开 H5 链接**。
2.  系统检测 IndexedDB，若有数据则展示 Loading 动画后加载列表（F3），若无数据则展示空状态（F1）。
3.  **用户点击“添加基金”**，跳转至添加页（F2）。
4.  用户输入代码 `000001` 和份额 `1000`，系统自动查询到名称“华夏成长混合”，用户点击保存，数据写入 IndexedDB。
5.  系统跳回主页，并在列表中显示该基金的最新估值和盈亏。
6.  **用户想看走势**，点击该基金卡片，进入详情页（F4）查看折线图。
---
## 5. 非功能需求
*   **性能要求**：首屏加载时间需在 1.5 秒以内（利用 CDN 加速 Vue/ECharts 等库）。IndexedDB 异步读取需配合 Loading 状态优化体验。
*   **兼容性要求**：
    *   必须完美适配微信内置浏览器。
    *   兼容 iOS Safari 和 Android Chrome（需确保 IndexedDB 兼容性，现代浏览器均支持）。
*   **安全性要求**：
    *   全站 HTTPS（GitHub Pages 默认支持）。
    *   **数据隐私**：明确提示用户数据仅保存在本地手机浏览器的 IndexedDB 中，清空缓存会丢失数据。
*   **可用性要求**：
    *   支持下拉刷新（可选优化）。
    *   数字字体需清晰，涨跌幅颜色需符合国内股市习惯（红涨绿跌）。
---
## 6. 数据需求
### 6.1 数据来源
*   **实时估值/涨跌幅**：使用第三方免费基金 API（如腾讯财经基金接口 `fundgz.1234567.com.cn` 或 新浪基金接口）。
*   **历史净值**：使用第三方基金历史数据接口（如东方财富接口），注意解决跨域（CORS）问题。
### 6.2 数据库设计
采用 **IndexedDB** 进行本地持久化存储，数据库结构设计如下：
*   **数据库名称**：`FundMonitorDB`
*   **版本号**：`1`
*   **对象仓库**：
    1.  **`funds` 仓库**（存储用户自选基金列表）：
        *   **主键**：`fund_code` (字符串，基金代码)
        *   **索引**：`add_time` (时间戳，用于排序，可选)
        *   **存储结构示例**：
            ```json
            {
              "fund_code": "000001",
              "fund_name": "华夏成长混合",
              "holding_amount": 1000.00,
              "add_time": 1678900000000
            }
            ```
    2.  **`history_cache` 仓库**（可选，存储历史净值缓存，减少接口请求）：
        *   **主键**：`fund_code`
        *   **存储结构示例**：
            ```json
            {
              "fund_code": "000001",
              "data": [
                  {"date": "2023-01-01", "value": 1.5},
                  {"date": "2023-01-02", "value": 1.52}
              ], 
              "last_update": 1678900000000
            }
            ```
---
## 7. 部署与运维方案
*   **代码托管**：GitHub。
*   **静态托管**：GitHub Pages 服务。
*   **域名（可选）**：通过 GitHub Pages 绑定自定义域名（如 `fund.mydomain.com`），如果不绑定则使用默认 `username.github.io`。
*   **访问限制**：完全公开访问，无登录鉴权。
---
## 8. 风险与备注
1.  **接口稳定性**：免费接口可能会限制频率或停止服务，需在代码中预留接口切换的灵活性，或编写简单的降级逻辑（当接口失败时显示“--”）。
2.  **跨域问题**：H5 直接请求第三方接口极易遇到跨域（CORS）错误。
    *   *解决方案A*：寻找支持 JSONP 的接口（如腾讯接口）。
    *   *解决方案B*：使用 CORS 代理服务（如 `cors-anywhere`）。
    *   *解决方案C（推荐）*：由于 GitHub Pages 是静态的，若所有接口都有跨域限制，可考虑使用 Vercel/Netlify 的 Serverless Function 做一层轻量级转发。
3.  **数据丢失与备份**：
    *   IndexedDB 数据与浏览器缓存绑定，用户清除浏览器数据或更换设备/浏览器会导致数据丢失。
    *   **新增功能建议**：利用 IndexedDB 易于导出的特性，建议在 V1.1 版本增加“导出配置”功能（将 DB 数据导出为 JSON 文件）和“导入配置”功能，方便用户跨设备迁移数据。
---
## 9. 给开发者的技术备注
为了简化 IndexedDB 的原生 API 调用（通常较繁琐），建议在开发时引入轻量级库，如：
*   **Dexie.js**: 这是一个非常流行的 IndexedDB 封装库，语法简洁，支持 Promise，能大幅降低开发成本。
*   **idb**: 这是一个由 Jake Archibald (Chrome 团队成员) 维护的微型封装库，更贴近原生 API 但解决了回调地狱问题。
**示例逻辑 (使用 Dexie.js):**
```javascript
// 定义数据库
const db = new Dexie('FundMonitorDB');
db.version(1).stores({
    funds: 'fund_code, add_time', // fund_code 是主键
    history_cache: 'fund_code'
});
// 添加基金
async function addFund(fund) {
    await db.funds.put({
        fund_code: fund.code,
        fund_name: fund.name,
        holding_amount: fund.amount,
        add_time: Date.now()
    });
}
// 获取所有基金
async function getAllFunds() {
    const allFunds = await db.funds.toArray();
    return allFunds;
}
```
---